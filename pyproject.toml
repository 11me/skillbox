[project]
name = "claude-skillbox"
version = "0.7.0"
description = "Personal skills marketplace for Claude Code"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
